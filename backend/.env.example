# Recipient
CONTACT_TO_EMAIL=penselverket@hotmail.com

# SMTP connection (optional; without these the API runs in honest preview mode)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=webbplats@example.com
SMTP_USE_TLS=true

# Comma-separated hosts for production
ALLOWED_HOSTS=localhost,127.0.0.1
